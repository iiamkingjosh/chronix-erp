/**
 * Application release version — align `APP_VERSION_SEMVER` with `package.json` "version" when cutting releases.
 */
export const APP_VERSION_SEMVER = "2.0.0";

/** Shown in chrome (sidebar, login badge, portal): major.minor only */
export const APP_VERSION_SHORT_LABEL = "v2.0";
